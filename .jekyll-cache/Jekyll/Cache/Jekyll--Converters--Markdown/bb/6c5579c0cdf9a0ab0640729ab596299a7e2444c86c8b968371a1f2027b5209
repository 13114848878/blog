I"�<h3 id="数据从串口到网页">数据从串口到网页</h3>
<p>通过串口实时读取的数据，通过网页进行数据的展示。</p>

<h3 id="websocket-方法">websocket 方法</h3>
<p>通过 python、node.js 等读取串口的数据，将数据通过 websocket 传输给网页（通过 Javascript 接收），并实时显示。
我由于之前用 matlab 写了串口读取的程序，就直接用 matlab 来做服务器了。有大佬写了 <a href="https://github.com/jebej/MatlabWebSocket">matlabWebSocket</a> ，你可以从 github 上下载 clone ，如果你觉得太慢的话，我已经把该<a href="https://gitee.com/Wakes_T/MatlabWebSocket">项目</a>转移到了码云，同样方法进行clone。安装方式详见项目说明。</p>
<h5 id="服务端matlab代码">服务端（matlab）代码</h5>

<div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">a</span> <span class="o">=</span> <span class="n">rand</span><span class="o">(</span><span class="mi">1</span><span class="o">,</span> <span class="mi">1000</span><span class="o">);</span>
<span class="n">b</span> <span class="o">=</span> <span class="n">num2str</span><span class="o">(</span><span class="n">a</span><span class="o">,</span> <span class="err">'</span><span class="o">%</span><span class="mf">3.5f</span><span class="o">,</span><span class="err">'</span><span class="o">);</span>

</code></pre></div></div>

:ET